# Mini-Reverse-Image-Search-using-VGG16
A mini reverse image search system using VGG16 and cosine similarity. It extracts feature embeddings from food images, compares uploaded images with the dataset, and displays the most similar results through a simple Flask web app.
